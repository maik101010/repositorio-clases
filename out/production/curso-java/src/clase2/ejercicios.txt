1- Intentar implementar el compareTo de manera funcional, con expresiones lambda

2- Objetivo: Utilizar Set para realizar operaciones de unión e intersección de dos conjuntos.

    Crea dos Set<Integer> con algunos elementos.
    Crea un método que calcule la unión de ambos conjuntos y devuelva un Set<Integer>.
    Crea otro método que calcule la intersección de ambos conjuntos y devuelva un Set<Integer>.

input:
 1, 2, 3, 4
 3, 4, 5, 6

output:
1,2,3,4,5,6

3- Objetivo: Utilizar un Map para agrupar estudiantes según su calificación.

    Crea una clase Estudiante con atributos String nombre y int nota.
    Crea un método que reciba una lista de estudiantes (List<Estudiante>) y devuelva un Map<Integer, List<String>>,
    donde la clave es la nota y el valor es una lista de nombres de los estudiantes que obtuvieron esa nota.
    recorre la lista de estudiantes y agrúpalos en el mapa según su calificación.

Mostrar en pantalla quieres aprueban con mas de 5 o desaprueban

4-
    Crea un método que reciba dos listas de enteros (List<Integer> lista1 y List<Integer> lista2).
    Convierte las listas en Set<Integer>.
    Verifica si existe alguna intersección entre los dos conjuntos.
    El método debe devolver true si hay algún elemento común entre las dos listas, y false si no lo hay.
input: List<Integer> lista1 = Arrays.asList(1, 2, 3, 4, 5);
       List<Integer> lista2 = Arrays.asList(5, 6, 7, 8, 9);
output: true

input: List<Integer> lista3 = Arrays.asList(10, 11, 12);
       List<Integer> lista4 = Arrays.asList(13, 14, 15);
output: false